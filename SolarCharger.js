function idea_list() {
    var coworkers = ['날개', '2', 'me', 'you']
            var i = 0;
            while (i < 4) {
                document.write('<li>' + coworkers[i] + '</li>')
                i += 1
            }
}